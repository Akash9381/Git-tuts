git status
git --version

git add .a -all file are moved in staging area

git commit -m "version 1"   (for commit the files)

git log   (to find what chnged by employee's)

to set email
 git config --global user.email "aksharma@gmail.com"

to set user name
 git config --global user.name "Akash Kumar"

git commit -m "version 1"


three layeer stages

working   staging    commit

if no change then we check what's changed 
git diff filename

check after add to file changed
git diff --staged filename

(to show what types of changed)


for unstage used this keys
git reset filename


